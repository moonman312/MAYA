/**
 * Thin re-export of the Booking Speed observation (canonical implementation
 * lives in supabase/functions/_shared/observations/booking-speed.ts — see
 * the layering notes there). Mirrors src/lib/pms/redact.ts.
 */
export * from "../../../supabase/functions/_shared/observations/booking-speed";
